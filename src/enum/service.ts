export enum SERVICE_STATUS {
    ACTIVE = 'ACTIVE',
    PAUSED = 'PAUSED',
    ARCHIVED = 'ARCHIVED',
}

export enum SERVICE_PRICING_TYPE {
    DAILY = 'DAILY',
    HOURLY = 'HOURLY',
    PACKAGE = 'PACKAGE',
}

export enum SERVICE_LOCATION_TYPE {
    ONSITE = 'ONSITE',
    REMOTE = 'REMOTE',
}
